<h2>Doctor: <%= @doctor.name %></h2>

Appointments: <br>
<ul>
<% @doctor.appointments.each do |a| %>
    <%= link_to a.appointment_datetime.strftime("%B %d, %Y at %k:%M"), appointment_path(a) %><br>
    <%= link_to a.patient.name, patient_path(a.patient) %><br></br>
<% end %>    

</ul>